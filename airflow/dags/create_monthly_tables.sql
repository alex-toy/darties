CREATE TABLE IF NOT EXISTS public.staging_monthly_ca_fours (
	villes varchar(256),
	annee int4,
	lib_mois varchar(30),
	o_mois decimal(16,8),
	r_mois decimal(16,8)
);


CREATE TABLE IF NOT EXISTS public.staging_monthly_v_fours (
	villes varchar(256),
	annee int4,
	lib_mois varchar(30),
	o_mois decimal(16,8),
	r_mois decimal(16,8)
);


CREATE TABLE IF NOT EXISTS public.staging_monthly_mb_fours (
	villes varchar(256),
	annee int4,
	lib_mois varchar(30),
	o_mois decimal(16,8),
	r_mois decimal(16,8)
);




CREATE TABLE IF NOT EXISTS public.staging_monthly_ca_hifi (
	villes varchar(256),
	annee int4,
	lib_mois varchar(30),
	o_mois decimal(16,8),
	r_mois decimal(16,8)
);


CREATE TABLE IF NOT EXISTS public.staging_monthly_v_hifi (
	villes varchar(256),
	annee int4,
	lib_mois varchar(30),
	o_mois decimal(16,8),
	r_mois decimal(16,8)
);


CREATE TABLE IF NOT EXISTS public.staging_monthly_mb_hifi (
	villes varchar(256),
	annee int4,
	lib_mois varchar(30),
	o_mois decimal(16,8),
	r_mois decimal(16,8)
);




CREATE TABLE IF NOT EXISTS public.staging_monthly_ca_magneto (
	villes varchar(256),
	annee int4,
	lib_mois varchar(30),
	o_mois decimal(16,8),
	r_mois decimal(16,8)
);


CREATE TABLE IF NOT EXISTS public.staging_monthly_v_magneto (
	villes varchar(256),
	annee int4,
	lib_mois varchar(30),
	o_mois decimal(16,8),
	r_mois decimal(16,8)
);


CREATE TABLE IF NOT EXISTS public.staging_monthly_mb_magneto (
	villes varchar(256),
	annee int4,
	lib_mois varchar(30),
	o_mois decimal(16,8),
	r_mois decimal(16,8)
);




